import { Outlet } from "react-router-dom";
import styles from "./HomePage.module.css"

const Home = () => {
    return(
        <main>
            Hello
        </main>
    )
};

export default Home;
